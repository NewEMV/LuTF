rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Função para verificar se é admin
    function isAdmin() {
      return request.auth != null && 
             firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Eventos: leitura pública, upload apenas admin
    match /eventos/{eventoId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Posts: leitura pública, upload apenas admin
    match /posts/{postId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Vídeos: leitura pública, upload apenas admin
    match /videos/{videoId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Galeria: leitura pública, upload apenas admin
    match /galeria/{albumId}/{allPaths=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Users: leitura pública, upload apenas o próprio usuário ou admin
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth.uid == userId || isAdmin();
    }
  }
}
